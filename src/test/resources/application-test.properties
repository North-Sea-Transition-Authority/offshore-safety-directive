# Using H2 for the tests as a temporary measure until a final decision is made on which database is being used.
# This will be updated/removed when https://ogajira.atlassian.net/browse/OSDOP-204 is picked up.
database.url=jdbc:h2:mem:testdb
schema.password=
spring.datasource.driver-class-name=org.h2.Driver

energy-portal.registration-url="https://portal-registration-url"
energy-portal.logout-url="https://portal-logout-url"

# File upload
file-upload.max-file-upload-bytes=52428800
file-upload.allowed-file-extensions-csv=.bmp, .doc, .docx, .jpeg, .jpg, .pdf, .png, .txt, .xls, .xlsx